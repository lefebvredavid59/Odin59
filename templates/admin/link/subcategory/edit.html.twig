{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition {{ subcategory.name }}{% endblock %}

{% block body %}
<div class="container">
    <h1 class="page-title text-center">Edition {{ subcategory.name }}</h1>

    {{ include('admin/link/subcategory/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('subcategory_index') }}" class="btn btn-outline-primary mt-3">Retour à la
        liste</a>

    {{ include('admin/link/subcategory/_delete_form.html.twig') }}
</div>
{% endblock %}
