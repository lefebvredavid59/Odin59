<form method="post" action="{{ path('value_delete', {'id': value.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ value.id) }}">
    <button class="btn btn-danger btn-block mt-3">Effacer</button>
</form>
