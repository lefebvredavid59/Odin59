{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition {{ value.name }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="page-title text-center">Edition {{ value.name }}</h1>

        {{ include('admin/link/value/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('value_index') }}" class="btn btn-outline-primary mt-3">Retour à la
            liste</a>

        {{ include('admin/link/value/_delete_form.html.twig') }}
    </div>
{% endblock %}
