{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition {{ category.name }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="page-title text-center">Edition {{ category.name }}</h1>

        {{ include('admin/link/category/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('category_index') }}" class="btn btn-outline-primary mt-3">Retour à la
            liste</a>

        {{ include('admin/link/category/_delete_form.html.twig') }}
    </div>
{% endblock %}
