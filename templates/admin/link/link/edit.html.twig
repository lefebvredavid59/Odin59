{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition {{ link.name }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="page-title text-center">Edition {{ link.name }}</h1>

        {{ include('admin/link/link/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('link_index') }}" class="btn btn-outline-primary mt-3">Retour à la
            liste</a>

        {{ include('admin/link/link/_delete_form.html.twig') }}
    </div>
{% endblock %}
