{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition {{ user.pseudo }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="page-title text-center">Edition {{ user.pseudo }}</h1>

        {{ include('admin/site/user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('user_index') }}" class="btn btn-outline-primary mt-3">Retour à la
            liste</a>

        {{ include('admin/site/user/_delete_form.html.twig') }}
    </div>
{% endblock %}
