{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} Edition {{ support.name }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="page-title text-center">Edition {{ support.name }}</h1>

        {{ include('admin/site/support/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('support_index') }}" class="btn btn-outline-primary mt-3">Retour à la
            liste</a>

        {{ include('admin/site/support/_delete_form.html.twig') }}
    </div>
{% endblock %}
